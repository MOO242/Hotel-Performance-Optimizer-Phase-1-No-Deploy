name: 'hotel_optimizer'
version: '1.0.0'
profile: 'hotel_optimizer'

model-paths: ["models"]

models:
  hotel_optimizer:
    staging:
      +materialized: view
    marts:
      +materialized: table
    metrics:
      +materialized: table
